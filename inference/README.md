# Inference

Local inference runtimes and adapters for CPU, GPU, WebGPU, WASM, desktop, and other supported environments.

Inference code should consume model manifests rather than hard-code a single language or framework model.
