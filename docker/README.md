# Docker

Container definitions for reproducible development, dataset ingestion, training, evaluation, and inference environments.

Keep runtime-specific images separate so model training dependencies do not inflate local inference images.
