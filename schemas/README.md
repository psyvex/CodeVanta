# Shared schemas

Machine-readable contracts shared by the Rust engine, language integrations, training/evaluation tooling, and future clients.

Schema changes should be versioned deliberately because findings and dataset records may be persisted or exchanged between independently released components.
