# Dataset Core

Shared normalized training-example primitives used by ingestion adapters.

This layer is intentionally independent of a specific programming language. Language and framework context belongs in ecosystem metadata, while source acquisition belongs in ingestion adapters.
