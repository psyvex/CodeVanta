# Analyzers

Deterministic software-analysis integrations used alongside CodeVanta models.

Examples include AST analysis, type checking, linting, dependency scanning, security scanners, complexity metrics, formatters, and test tooling. AI should complement these analyzers rather than replace them when deterministic analysis is more reliable.
