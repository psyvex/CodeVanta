{
  "name": "@codevanta/platform",
  "version": "0.1.0",
  "private": true,
  "description": "Local, language-specific model platform for software quality analysis.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test --import tsx tests/**/*.test.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
